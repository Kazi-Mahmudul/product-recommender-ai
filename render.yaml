services:
  - type: web
    name: pickbd-ai
    env: python
    buildCommand: pip install -r requirements.production.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: product-recommender-db
          property: connectionString
      - key: API_V1_STR
        value: /api/v1
      - key: PROJECT_NAME
        value: Product Recommender API
      - key: BACKEND_CORS_ORIGINS
        value: '["http://localhost:3000","http://localhost:8000","http://localhost:8080"]'
      - key: SECRET_KEY
        generateValue: true
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 30

databases:
  - name: pickbd-db
    databaseName: product_recommender
    user: product_user
    plan: free

# We don't define the database here as it's external
# databases:
#   - name: pickbd-db
#     databaseName: product_recommender
#     user: product_user
#     plan: free
#     ipAllowList: [] 